---
'@monocloud/auth-node-core': minor
'@monocloud/backend-node': minor
'@monocloud/auth-nextjs': minor
---

Require Node.js >= 20. This matches the minimum Node version already required by the SDKs' dependencies (e.g. `joi` 18) and the versions tested in CI.

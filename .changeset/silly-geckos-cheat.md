---
'@monocloud/auth-node-core': patch
'@monocloud/auth-nextjs': patch
---

 - Added `refetchUserInfo` option to `getSession()` which will fetch the user profile from userinfo endpoint and update the session.
